var Promise = Promise || require("bluebird");

module.exports = function (knex) {
	this.knex = knex;

	return {
		getUser: function () {
			return knex("users")
			.then(function (rows){
				if (rows.length > 0) {
					return rows[0];
				} else {
					return false;
				}
			});
		}
	}
};