var Promise = Promise || require("bluebird");

module.exports = function (knex) {
	this.knex = knex;

	return {
		getUser: function () {
			return knex("users")
			.then(function (rows){
				return rows[0];
			});
		}
	}
};